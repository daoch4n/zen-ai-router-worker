# Task ID: 7
# Title: Export `handleRawTTS` from `src/handlers/index.mjs`
# Status: done
# Dependencies: 6
# Priority: medium
# Description: Ensure the newly created `handleRawTTS` function is properly exported from `src/handlers/tts.mjs` and re-exported via `src/handlers/index.mjs` so it can be imported by the main worker.
# Details:
Verify that `export async function handleRawTTS(...)` is present in `src/handlers/tts.mjs`. Confirm that `src/handlers/index.mjs` includes `export * from './tts.mjs';` or explicitly `export { handleRawTTS } from './tts.mjs';` to make it accessible to `src/worker.mjs`.

# Test Strategy:
Perform a simple import test in a temporary file or a build step to ensure `handleRawTTS` can be successfully imported from `src/handlers/index.mjs` without module resolution errors.
